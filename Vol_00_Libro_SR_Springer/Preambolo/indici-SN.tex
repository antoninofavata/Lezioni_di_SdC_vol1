% ============================================================================
%  indici-SN.tex
%  Indice analitico ed elenco dei simboli per SNmono.
%
%  NOTA: rimosso imakeidx (conflitti con SNmono).
%        L'indice analitico si genera con il makeindex standard
%        e il file di stile svind.ist (dal template Springer).
%        Il comando \makeindex è già nel main.
% ============================================================================

% ── Indice analitico ─────────────────────────────────────────────────────
% \makeindex è nel main_Vol_00.tex — usare svind.ist in compilazione:
%   makeindex main_Vol_00.idx -s svind.ist

\renewcommand{\seename}{vedi}
\providecommand{\seealso}[2]{\emph{vedi anche} #1}

% ── Elenco dei simboli (nomencl) ─────────────────────────────────────────
\usepackage[italian]{nomencl}
\renewcommand\nomgroup[1]{%
  \item[\bfseries
  \ifstrequal{#1}{L}{Simboli latini}{%
    \ifstrequal{#1}{G}{Simboli greci}{%
      \ifstrequal{#1}{O}{Operatori e simboli speciali}{%
        \ifstrequal{#1}{S}{Spazi e insiemi}{}}}}]}
\setlength{\nomitemsep}{2pt}
\makenomenclature
