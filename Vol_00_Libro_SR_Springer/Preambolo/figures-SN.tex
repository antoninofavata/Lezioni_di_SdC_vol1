% ============================================================================
%  figures-SN.tex
%  Configurazione figure compatibile con SNmono.
%
%  NOTA: NON ricaricare \usepackage{caption} — SNmono lo gestisce già.
%        NON ricaricare \usepackage{graphicx} — già nel main con SNmono.
% ============================================================================

\usepackage{subcaption}
\usepackage[innercaption]{sidecap}
\usepackage{wrapfig}
\usepackage{rotating}
\usepackage{float}
%\usetikzlibrary{calc}

% Comando per numeri con circolo perimetrale
\usepackage{circledsteps}

% Posizione caption laterale
\sidecaptionvpos{figure}{t}

% Personalizzazione caption (DOPO SNmono, quindi qui è sicuro)
% SNmono usa già footnotesize per le caption; decommentare solo se
% si vuole sovrascrivere.
%\captionsetup{
%  font=footnotesize,
%  labelfont={it,bf},
%  textfont=rm,
%  format=plain,
%  labelsep=period,
%  margin=10pt,
%  indention=0.5cm
%}
